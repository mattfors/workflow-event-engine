# workflow-event-engine

Event-driven workflow engine with projected state for pick execution.

## Run demo

From repo root:

- `npm install`
- `npm run demo`

Alternative workspace command:

- `npm run demo:ws`